- name: "Neovim | Debian-family | Dependencies"
  ansible.builtin.apt:
    name:
      - cmake
      - curl
      - pkg-config
      - libtool
      - unzip
      - ripgrep
    state: present
  become: true

- name: "Neovim | Debian-family | Install"
  ansible.builtin.apt:
    name: neovim
    state: present
  become: true

